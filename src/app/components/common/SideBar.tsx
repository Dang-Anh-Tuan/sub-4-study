import { FC } from 'react'

interface SideBarProps {}

const SideBar: FC<SideBarProps> = ({}) => {
  return <div className='top-0 left-0 w-[300px] h-[100vh] bg-slate-400'></div>
}

export default SideBar
